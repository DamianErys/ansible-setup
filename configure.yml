# ============================================================
# configure.yml
# Configures apps and the terminal environment.
# ============================================================
- name: Configure
  hosts: "{{ machine_type }}"
  become: true
  vars_files:
    - vault.yml
  roles:
