# ============================================================
# desktop.yml
# Desktop environment — folders, theme, GNOME settings.
# ============================================================
- name: Desktop
  hosts: "{{ machine_type }}"
  become: true
  vars_files:
    - vault.yml
  roles:
    - 18_app-organise
