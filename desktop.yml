# ============================================================
# desktop.yml
# Desktop environment — folders, theme, GNOME settings.
# ============================================================
- name: Desktop
  hosts: "{{ machine_type }}"
  become: true
  roles:
