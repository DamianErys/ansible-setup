# ============================================================
# site.yml
# Master playbook — runs all four in order.
# Comment out any playbook to skip it during testing.
# ============================================================

# - import_playbook: build-base.yml
- import_playbook: build-vms.yml
- import_playbook: configure.yml
# - import_playbook: desktop.yml
# - import_playbook: connectivity.yml
