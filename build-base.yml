# ============================================================
# build-base.yml
# Brings the system to a clean, up-to-date baseline.
# ============================================================
- name: Build Base
  hosts: "{{ machine_type }}"
  become: true
  vars_files:
    - vault.yml
  roles:
    # - 1_remove-packages      # remove bloat first
    # - 2_update-system        # then update what remains
    - 3_all-packages     # then install flatpaks
    # - 4_asus-zenscreen       # then drivers